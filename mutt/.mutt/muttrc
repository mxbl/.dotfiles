# IMAP
set imap_user        = block@math.tu-berlin.de
set spoolfile        = imaps://block@mail.math.tu-berlin.de/Inbox
set header_cache     = "~/.mutt/cache/headers"
set message_cachedir = "~/.mutt/cache/bodies"
set certificate_file = "~.mutt/certificates"

# SMTP
set smtp_url = "smtp://block@mail.math.tu-berlin.de:587/"

# Composition
set editor       = "nvim"
set edit_headers = yes
set charset      = UTF-8

set realname = "Maximilian Block"
set from     = "max@pool.math.tu-berlin.de"
set use_from = yes
